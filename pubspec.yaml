name: kingdom_sponsor_app
description: "Kingdom Sponsor - neutral fundraising platform app."
publish_to: 'none'
version: 0.2.0

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter

  flutter_riverpod: ^3.2.1
  go_router: ^17.1.0
  http: ^1.6.0
  flutter_secure_storage: ^9.2.4
  google_fonts: ^8.0.1
  lucide_icons: ^0.257.0
  flutter_dotenv: ^5.2.1
  intl: ^0.20.2
  shimmer: ^3.0.0
  url_launcher: ^6.3.2
  qr_flutter: ^4.1.0
  image_picker: ^1.2.2
  http_parser: ^4.1.2
  firebase_core: ^3.13.0
  firebase_messaging: ^15.2.5
  sentry_flutter: ^7.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - .env
